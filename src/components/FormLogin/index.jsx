import { FormContainer, Title, InfoInput, InputContainer, Input,Button } from "./style";

const FormLogin = () => {
    return(
        <>
            <FormContainer>
                

            </FormContainer>
        </>
    )
}

export default FormLogin;